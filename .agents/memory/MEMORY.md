- [ISBE Class Size Report loading](il-classsize-loader.md) — pandas MultiIndex header needs "Unnamed:" token filtering when flattening District Data sheet
- [IL cost-impact estimate](il-cost-impact.md) — formula, joins, and NULL-safety rules for est_annual_cost_impact on IL settlements
- [Pipeline workflows on-demand](pipeline-workflows-on-demand.md) — IL crawl/extraction/audit REMOVED from workspace (auto-started→starved dev, crashed app); run via admin POST endpoints, never re-add as workflows.
- [IL crawl discovery boosts](il-crawl-discovery-boosts.md) — sitemap + NixOS Playwright JS-render + on-page doc discovery; %PDF-verify before find; html_contract source_type plumbed crawler→DB→extractor.
- [Replit background processes](replit-bg-processes.md) — Replit kills detached/nohup/fork child processes between tool calls; only managed workflows survive long-running tasks.
- [ISBE directory XLS format](isbe-directory-format.md) — sheet "1 Public Dist & Sch"; RCDTS = 9-digit RCD col + 2-digit Type col = 11 digits; filter RecType=='Dist' AND School=='0000'.
- [IL CBA search fallback](il-cba-search-fallback.md) — Serper (primary) → Google CSE → DuckDuckGo; SERPER_API_KEY now set; run --search-fallback after direct crawl finishes.
- [Cron requires Reserved VM](cron-reserved-vm.md) — node-cron fires only on Reserved VM; deployment type is set in the Replit Publishing UI (serve="vm" is NOT a valid artifact.toml value).
- [Deployment image >8 GiB](deployment-image-size.md) — publish rejected for image size; trim .git/.cache/pipeline/data via .replitignore (safe on autoscale: pipeline/PDFs not used at runtime).
- [ISBE connectivity from repl](isbe-connectivity.md) — isbe.net is unreachable from the sandbox (ConnectTimeoutError); only testable once deployed on a Reserved VM.
- [Deployment CWD vs dev CWD](deployment-cwd-paths.md) — prod runs API server from workspace root, so process.cwd()+"../../pipeline" breaks ("/home/pipeline"); resolve repo-root paths by walking up.
- [Dev schema apply + prod publish](drizzle-push-prod-schema.md) — never DDL prod; apply additive cols to dev DB then re-publish. `db push` wants to TRUNCATE contracts — use surgical ALTER instead.
- [CollBar district id types](collbar-district-id-types.md) — dashboard API returns district id as STRING (coerce when matching numeric auth districtId); list endpoint capped at LIMIT 1000, fetch a specific district via /:id.
- [extraction_runs is append-per-attempt](il-extraction-runs-latest-per-doc.md) — one row per attempt; use latest-per-doc (DISTINCT ON source_doc_id) for status, never raw COUNT by status.
- [IL CBA crawl precision](il-crawl-precision.md) — denylist aggregator/gov domains in name-search; require %PDF bytes before storing (HTML-as-PDF falsely marks districts found).
- [Derive vs unit reclassification](il-extraction-derive-savepoint.md) — backfill_contract_units must wrap each UPDATE in a SAVEPOINT or a unique-constraint conflict aborts the whole derive transaction.
- [Schema apply + drift guardrail](drizzle-push-unique-constraints.md) — hybrid DB (migrations + runMigrations + non-Drizzle tables) makes drizzle-kit push/push-force unusable; push-force neutered; verify schema with check-drift.
- [Bargaining-unit scoping](bargaining-unit-scoping.md) — benchmarks never mix units, default teachers; whole customer Overview (detail/provisions/settlements/provision-medians) unit-scoped; parseUnit whitelist; provenance own-doc→same-unit fallback; cost-impact teachers-only; contracts-unique gap.
- [OCR quality flag](ocr-quality-flag.md) — extraction_runs.ocr_confidence/ocr_low_quality (threshold 70); use latest-per-doc; legacy cache has no score (NULL != good).
- [IL viewer recovery](il-viewer-recovery.md) — manual-review CSV is a crawl side-effect (empty until non-dry-run re-crawl); 13_recover_viewer_cbas.py resolves Box/Issuu/Drive best-effort + --pdf manual ingest.
- [Board-policy manual purge](policy-manual-purge.md) — non-CBA PRESS manuals stored as cba_pdf; re-label to doc_type 'policy_manual' + purge derived rows; scanned ones caught by URL not OCR.
- [Free-tier access gating](free-tier-gating.md) — gate() reads live DB access; free=own Overview+Toolkit only; aggregate medians need ownFilters or free users reconstruct paid Comparables.
- [IL ELRB final-offers pipeline](il-final-offers-pipeline.md) — board-vs-union scrape/extract/diff; doc_type 'final_offer'; IL-scoped reads; cron Phase E is idempotent-by-skip and won't re-extract edited PDFs.
- [IL min teacher salary ingest](il-min-teacher-salary.md) — CGFA cert filename year N = cert calendar year → effective school year (N+1)-(N+2); annual ingest via 17_sync_il_min_salary.py / admin run-min-salary-sync.
- [Ask endpoint SSE streaming](ask-sse-streaming.md) — /dashboard/ask streams SSE; model emits preamble before tools so keep the `reset` event; abort stream on req close; no compression or it buffers.
- [Customer-view state scoping](collbar-customer-state-scoping.md) — customer dashboard is IL-only via CUSTOMER_STATE; per-id child endpoints need their own guard or OH leaks (IDOR); OH kept in DB.
- [Stored CBA audit OCR-recheck](cba-audit-recheck.md) — force-OCR recheck: OCR-fail must label 'unreadable', never fall back to a confident 'not-CBA' from the distrusted thin text layer.
- [doc_type purge constraint lockstep](doc-type-purge-constraint-lockstep.md) — 15/16 purge scripts each DROP/ADD the doc_type CHECK from their own _DOC_TYPES; keep all lists + schema in lockstep or the later --apply fails.
- [Extraction domain CHECK drift](extraction-domain-check-constraints.md) — live extraction_jobs/_versions/_promotions domain CHECKs lag the VersionDomain type; new domain needs all 3 ALTERed in runMigrations + CREATE defs or 23514 at runtime.
- [Non-CBA cleanup confident vs borderline](non-cba-cleanup-split.md) — 16_purge_non_cbas relabels confident not-CBA→non_cba; CBA-named/thin rows HELD to non_cba_review.csv, never bulk-deleted.
- [Crawler content gate (cba_pdf)](crawler-content-gate.md) — crawler classifies PDF text-layer before storing; reject only readable confident non-CBAs, KEEP inconclusive (scanned CBAs) or you false-reject contracts.
- [api-server new-route 404 → restart](api-server-new-route-restart.md) — a correct, registered new route 404s when the tsx-watch dev server predates it; restart the API Server workflow before curl-testing.
- [Promote dev CBA data to prod](promote-dev-to-prod.md) — DEV→PROD copy by natural key (no id copy); contracts key must be 6-tuple or NULL effective_start collides under IS NOT DISTINCT FROM.
- [Board Packet PDF render bound](board-packet-pdf-render-bound.md) — cap PDF table to one row per district; full history blocks renderToBuffer/event loop; use peer-set district_ids only (no filters_json re-resolve).
- [IL CBA re-check expiring](il-recheck-expiring.md) — --recheck-expiring re-fetches only saved URL of expiring districts; crawl-state JSON must be atomic-written; current contract partitions by (district,unit,unit_scope).
- [Uploaded CBA units + source links](il-uploaded-cba-units-and-links.md) — upload:// admin unit authoritative (extractor honors, backfill skips); admin reassign pins contracts.unit_override (atomic→settlements+upload source_doc); authed PDF route (free=own district).
- [Uploaded CBA → Object Storage](il-uploaded-cba-object-storage.md) — uploaded PDFs MUST persist to Object Storage (il_cba/<hash>.pdf); local: is dev-only/ephemeral; upload must require the object write or prod 404s "Document file missing".
- [Review queue scope + audit sampling](review-queue-audit-sampling.md) — queue = confidence<0.8 AND NOT human_verified only; audit sampling disabled (rate 0); is_audit_sample is historical-only, never gates the queue.
- [CollBar web theming & a11y](collbar-web-theming-a11y.md) — shadcn theme tokens are placeholder `red` but never surface (app uses hardcoded slate/blue); only `--ring` is real; ui/* primitives are dead code; real modals are hand-rolled→Radix Dialog.
- [Google Drive connector proxy](drive-connector-proxy.md) — proxy is OAuth control-plane only (~1MB body→413, partial chunks→403); upload by opening a resumable session via proxy then PUTting bytes DIRECT to the googleapis.com session URI. Run probes from inside artifacts/api-server.
- [Source-PDF link auth](collbar-document-link-auth.md) — new-tab document links lose the SameSite=Lax iframe cookie; carry a signed per-user token (HMAC over userId+exp w/ SESSION_SECRET) verified server-side, access still re-checked.
- [Prod 300s timeout + json 100kb](prod-proxy-timeout-and-json-limit.md) — prod aborts requests >~300s (CPU-bound work must batch client-side); global express.json 100kb runs before routes so per-route limits are no-ops and 413s aren't JSON.
- [gate ownDistrict is free-only](gate-ownDistrict-free-only.md) — gate({ownDistrict}) skips the check for admins AND Pro users; district-scoped WRITE endpoints must re-check ownership in-handler or it's an IDOR.
- [Salary grid display](salary-grid-display.md) — gate Compensation grid on response.bargainingUnit===unit (placeholderData flashes teacher BA/MA on non-teacher units); education chrome gated by laneKind; some lane_grids have empty lane_labels → "Col N".
- [Salary extraction cross-unit leak](salary-extraction-cross-unit-leak.md) — one CBA PDF shared by multiple unit contract-rows makes 18_extract stamp the teacher grid onto support_staff/secretarial; add magnitude sanity; ~40% recall gap. Don't backfill until fixed.
- [Salary-grid textual Step N](salary-grid-textual-step.md) — textual "Step 1" rows must be an education-only fallback; non-edu textual grids (stipends) evade the magnitude floor and must be rejected; glued/corrupt labels rejected by construction.
- [Salary-grid coverage gap](salary-grid-coverage-gap.md) — IL teacher grid recall ~33%; readable-but-unrecognized misses are mostly prose/rosters/fragments not grids; real recoverable bucket is ~58 scanned PDFs needing OCR (separate task).
- [ISBE TSS & EIS loaders](isbe-salary-loaders.md) — TSS typed cols by EXACT header match + self-migrate; EIS 3 tables (district/educator/position) DELETE-by-year idempotent; never persist names; psycopg2 needs numpy→native (tolist + NaN→None).
- [Ask assistant tool scoping](ask-tools-scoping.md) — every query in a /dashboard/ask tool must independently re-assert d.state=CUSTOMER_STATE (+ c.bargaining_unit for unit data); never trust an upstream phase; tests enforce IL-anchoring.
- [In-process extraction engine](in-process-extraction-engine.md) — admin upload/re-run runs Claude Vision in-process; live tables = promoted projection (never extraction output); auto-promote only on first; fail-closed; REQUIRES always-on Reserved VM; dev worker runs (don't enqueue real jobs in dev).
- [Vision array-prompt parsing](vision-array-prompt-parsing.md) — array-returning vision/triage prompts must parse with tolerant extractJsonArray (handles fences/prose); object-wrap reconstruction false-fails-closed → "Not yet extracted" despite a good answer.
- [Scanned salary vision extraction](salary-vision-extraction.md) — Claude-vision fallback for scanned grids; NO salary review queue (only magnitude gate withholds), so needs_review/low-conf still surface; vision OCR ~99.8% non-deterministic; lane-row width + edu-label canon fail-closed.
- [Publish DB-diff timeout](publish-db-diff-timeout.md) — Replit publish schema-diff times out on cold Neon prod DB (~17s connect > check timeout); warm prod (SELECT 1, env=production) then retry publish within ~5min.
- [Dev login lock pileup](db-login-lock-pileup.md) — leaked idle-in-txn lock on `users` + boot ALTER's ACCESS EXCLUSIVE froze all logins; pool's idle_in_transaction_session_timeout fixes it; `test` workflow vs live dev DB is the likely trigger.
- [Project run button aggregate](project-run-button-aggregate.md) — "Project" runButton auto-aggregates ALL agent leaf workflows (autoStart:false doesn't exclude); can't edit it (prohibited name); artifact web/API workflows auto-start separately.
- [Mods not reaching prod = domain mapping](prod-domain-mapping-check.md) — successful publish ≠ visible; check getDeploymentInfo primaryUrl/additionalUrls; a 404 from the user's URL means no deployment is mapped to that hostname (linking issue, not a code bug).
- [TS vision extraction engine](ts-extraction-engine.md) — Claude-Vision extractors in api-server: every result path fail-closed (incl. scanned triage) or the delete-then-insert store wipes rows; dryRun until merge UX.
- [Salary hourly/annual mixed tables](salary-hourly-mixed-tables.md) — support wage tables mix hourly+annual columns; classify/format/sanity PER-LANE (hourly only when isHourly&&!isAnnual; annual unbounded); scheduleType hourly only if no annual lane; keep salary.ts↔district.tsx in lockstep.
- [Provisions per-page resilience](provisions-per-page-resilience.md) — a bad batch must fail at PAGE granularity (recurse-split to single page, skip irreducible page) not discard the whole doc; one-shot capped deep-retry when tier-1 yields zero.
- [ISBE salary-dataset admin upload](salary-dataset-upload.md) — EIS/TSS spreadsheet upload reuses min-salary detached-spawn pattern; TSS needs explicit school_year (no year column), EIS auto-detects; reject concurrent uploads before writing.
- [TSS field display semantics](tss-display-semantics.md) — tss_annual percents are 0–100; blank text flags ≠ "No"; fair_share/longevity_pay are opaque 1/2 codes; $0 premiums ambiguous.
- [Firm "Entire database" clause scope](firm-clause-database-scope.md) — clause scope is a districts|state discriminated union; "database"=whole CUSTOMER_STATE corpus; firm /document auth widened to firm-member + in-state (OH still 403).
- [Firm settlement alerts](firm-settlement-alerts.md) — global `alerts` table reused; new_settlement dedups on file_hash=sha256(content), new_doc on source_doc_id; detection piggybacks ingest (no cron); firm reads scoped to roster∪matters.
- [Firm work-product exports](firm-exports-citation-parity.md) — /app exports render ONLY from buildMatrix/buildClauseCompare (no new analysis, citation parity); upload-before-row durability; requireFirmSession + cross-firm 404; shared models keep route shapes in lockstep.
- [IL clause search & compare](il-clause-search-compare.md) — Phase 4 retrieval-first, firm-scoped (scope all=firm-only); synthesis grounded-only/non-blocking; two-query compare picker, clear type on scope change to avoid stray opus calls.
- [Firm compare provenance gating](firm-compare-provenance-gating.md) — show-cell gate=value+source_url(+verbatim excerpt for provisions); never gate page_ref/confidence (settlements ALL NULL page_ref; human-verified can lack confidence); pick latest settlement in CTE before citation join.
- [Firm workspace roster & matters](firm-workspace-matters.md) — firm_members/requireFirmSession is a SECOND entitlement system (≠ gate()/plan); all firm reads scoped to firmAccess.firmId (cross-firm→404); matter client dual-stored (primary_district_id + role='client' row) must stay in sync in a txn.
- [Firm invite is a bearer token](firm-invite-bearer-token.md) — accept attaches an EXISTING account only when already authed as that user (no password-bypass); new accounts get a password; recheck expiry under FOR UPDATE; never clobber existing role/plan/district.
- [Bulk CBA import (Drive→prod)](bulk-cba-import.md) — all enqueue paths behind one dev-safety gate; object-storage write on BOTH new+duplicate paths; progress/retry job SQL must scope domain='cba'.
- [Firm settlement alerts](firm-settlement-alerts.md) — reuse global alerts table; dedup machine-key in file_hash (NOT notes, admin ack overwrites it); feed by current subscription (late subs see history); detection piggybacks refresh (no cron); delete alerts before districts (no cascade).
- [Firm full-IL Settlements + Ask reuse](firm-full-il-reuse.md) — /app Settlements & Ask open to ALL firm members, full IL data; use requireFirmSession NEVER gate(); reuse district-reads.ts + ask-engine.ts (no fork); Ask ResultPathMode rewrites cards to /app/settlements; mount firm routes before dashboardRouter.
- [Anthropic proxy prompt caching](anthropic-proxy-prompt-caching.md) — Replit Anthropic proxy DOES accept cache_control + returns cache usage; Ask engine caches tools+system prefix, withTools-only, 400-fallback latch.
